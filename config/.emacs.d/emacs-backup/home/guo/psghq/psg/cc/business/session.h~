
#ifndef _PSG_CORE_FRAMEWORK_
#define _PSG_CORE_FRAMEWORK_

// scheduler <-- sessions

class Session {
public:
  void generate_events(); // edf -> events
  void push_events_from_user(); // events -> events'
  void generate_reports(); // events' -> reports
  
  std::string id; // hash of edf data

  std::vector<Events> events;
  std::vector<Report> reports;
  std::map<std::string, std::vector<float>> signals;
  // chart ... 
  
private:
  std::vector<int> stage;
  // ... temp data
};


#endif
