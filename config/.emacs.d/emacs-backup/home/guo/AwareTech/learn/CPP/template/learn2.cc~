#include <type_traits>
#include <iostream>

using namespace std;

template <class T, typename = typename enable_if<is_integral<T>::value, int>::type>
class A {
public:
  T a;
  A(int a): a{a} {}
};


int main() {
  A<int, int> a(1);
  

  return 0;
}
