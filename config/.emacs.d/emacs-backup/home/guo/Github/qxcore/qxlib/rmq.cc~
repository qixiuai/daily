#include <iostream>


using std::size_t;

template <type T>
class RMQ {
public:
  explicit RMQ(T* arr, std::size_t n);
  bool query();
  
private:
  T* arr;
  size_t n;
};


int main() {
  
  return 0;
}
