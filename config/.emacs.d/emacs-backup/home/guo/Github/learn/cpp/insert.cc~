
#include <iostream>
#include <vector>


int main() {
  std::vector<float> arr{0,1,2};
  std::vector<float> dest;
  
  dest.insert(dest.begin(), arr.begin(), arr.end());
  
  for (auto v : dest)
    std::cout << v << '\n';

  return 0;
}

