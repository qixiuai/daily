from django.urls import path

from . import views

app_name = "psg"

urlpatterns = [
    path("", views.IndexView.as_view(), name="index"),
    path("test/", views.get_user_option, name="handle_file"),
]
