
#include <iostream>
#include <thread>
#include <chrono>
#include "psg.h"

int main()
{
  physio::PSG psg;
  std::cout << "main" << std::endl;
  std::this_thread::sleep_for(std::chrono::seconds(1));
  return 0;
}
