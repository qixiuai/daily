
#include <cstring>
#include <vector>
#include <iostream>
#include <memory>
#include "tensorflow/c/c_api.h"

namespace physio {
  namespace stage {
    class Model {
    public:
      Model() = default;
      Model(const std::string& model_dir);
      std::string predict(const std::vector<float>& spectrogram);
    private:
      std::unique_ptr<TF_Session> session;
      const std::string& saved_model_dir;
    };
    Model::Model(const std::string& model_dir) {
      saved_model_dir = model_dir;
      // load model
    }
    std::string Model::predict(const std::vector<float>& spectrogram) {
      return "";
    }
  }
}
