
import numpy as np


def moveavg(signal, window_size):
    """ signal : numpy array """
    avg = np.zeros_like(signal)
    size = len(signal)
    for i in range(size):
        start = i - window_size if i > window_size else 0
        avg[i] = np.mean(signal[start:i])
    return avg
