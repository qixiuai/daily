#include <string>
#include "psg/api/data.h"

namespace psg {

  void add_pose_event(comm::DB& db, std::string hex_str, EdfPlus* edf) {
    // extract spo2_signal
    std::string signal_name = "Event pose";
    auto spo2 = edf->parseSingleSignal(3);
    db[hex_str].pose_signal = *pose;
  }

}
