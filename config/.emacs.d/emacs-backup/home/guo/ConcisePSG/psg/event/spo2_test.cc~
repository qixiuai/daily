
#include "psg/event/spo2.h"


int main() {
  std::vector<float> spo2 = {1,2,3,4,5,56,6,7};
  for (int i = 100; i < 10000; i++) {
    spo2.push_back(i);
  }
  psg::event::detect_desat(spo2);

  return 0;
}
