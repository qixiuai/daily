#include <boost/circular_buffer.hpp>

#include <iostream>

using  std::cout;

int main() {
  boost::circular_buffer<int> buf(4);
  buf.push_back(2);
  buf.push_back(4);

  for (const auto val : buf ) {
    cout << val << '\t';
  }
  cout << '\n';

  
  return 0;
}
