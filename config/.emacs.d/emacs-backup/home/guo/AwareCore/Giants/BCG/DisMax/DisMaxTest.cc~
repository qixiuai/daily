
#include <iostream>
#include <fstream>
#include <vector>
#include <string>

#include "DisMax.h"
//#include "gtest/gtest.h"


int main () {
  std::cout << "Start DisMaxTest...\n";

  std::string data_dir   = "/home/guo/AwareCore/Giants/data/";
  std::string test_file  = data_dir + "Test.txt";
  std::string sleep_file = data_dir + "sleep704.txt";  

  int Fz      = 100;
  auto config = DisMax::Config(Fz);
  //  auto alg    = DisMax::DisMaxAdjustWindowSize(config);

  auto data_stream = std::ifstream(test_file);
  int val;
  while (true) {
    data_stream >> val;
    if (val == 0)
      break;
  }
  
    
  return 0;
}
