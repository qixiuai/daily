// grpc service for signal transfer at Physio

syntax = "proto3";

package physio;

service SignalsService {
  // data server for consumers such as bcg alg
  rpc GetBCGSignal(ClientName) returns (stream BCGSignal) {}
}

message ClientName {
  string name = 1;
}

message BCGSignal {
  int32 val = 1;
}
