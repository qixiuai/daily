
#ifndef _PHYSIO_AWAKE_
#define _PHYSIO_AWAKE_

#include "dsa.h"
#include "utils.h"

namespace physio {

  class AwakeJudge {
  public:
    AwakeJudge() = default;
    AwakeJudge(EventQueues<int>& event_queues) {
      this->event_queues = event_queues;
    }
    int run(int eeg_f1_m2, int eeg_c3_m2, int eeg_c4_m1,
	    int eeg_f2_m1, int eeg_o1_m2, int eeg_o2_m1) {

      return 0;
    }
  private:
    EventQueues<int>& event_queues;
  };

}; // end namespace physio

#endif
