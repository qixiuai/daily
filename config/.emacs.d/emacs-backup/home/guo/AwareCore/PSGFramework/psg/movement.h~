#ifndef _PHYSIO_MOVEMENT_
#define _PHYSIO_MOVEMENT_

#include "dsa.h"
#include "utils.h"

namespace physio {
  class MovementJudge {
  public:
    MovementJudge() = default;
    MovementJudge(EventQueues<int>& event_queues) {
      this->event_queues = event_queues;
    }
    int run(int leg_l, int leg_r) {
      return 0;
    }
  private:
    EventQueues<int>& event_queues;
  };  

}; //end namespace physio

#endif
