#ifndef _PHYSIO_RESPIRATION_
#define _PHYSIO_RESPIRATION_

#include "dsa.h"
#include "utils.h"

namespace physio {
  class RespirationJudge {
  public:
    RespirationJudge() = default;
    RespirationJudge(EventQueues<int>& event_queues) {
      this->event_queues = event_queues;
    }
    int run(int resp_abdomen,  int resp_chest,
	    int resp_pressure, int resp_thermal) {
      
      return 0;
    }
  private:
    EventQueues<int>& event_queues;
  };  

}; //end namespace physio

#endif
