import numpy as np


if __name__ == '__main__':
    filename = "PressureMuzzleFlow_.dat"
    outname = "skipped_" + filename
    out = open(outname, 'wb')
    with open(filename, 'rb') as file:
        file.read(3)
        bytes = file.read()
        out.write(bytes)
    out.close()
    
