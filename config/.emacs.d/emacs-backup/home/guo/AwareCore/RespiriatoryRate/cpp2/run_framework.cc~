#include "respiratoryrate_wrapper.h"

#include <iostream>

int main() {
  auto p = respiratorydetector_construct(100);
  auto rate = respiratorydetector_run(p, 1);
  std::cout << rate << std::endl;
  respiratorydetector_destruct(p);
  return 0;
}
